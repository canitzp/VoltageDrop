package de.canitzp.cosmos.machine.spaceeye;

import de.canitzp.ctpcore.inventory.ContainerBase;
import net.minecraft.entity.player.EntityPlayer;

/**
 * @author canitzp
 */
public class ContainerSpaceEye extends ContainerBase<TileSpaceEye> {

    public ContainerSpaceEye(EntityPlayer player, int x, int y, int z) {
        super(player, x, y, z);
    }

}
